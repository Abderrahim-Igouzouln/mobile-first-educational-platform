import { Level } from '@prisma/client';

export const flutterAdvanced = {
  techSlug: "flutter",
  title: "Flutter : Applications complexes",
  description: "Maîtrisez le state management avec Bloc, intégrez Firebase, créez des animations fluides et déployez avec CI/CD.",
  level: Level.intermediate,
  durationMin: 300,
  lessons: [
  {
    "title": "State Management Avancé avec Bloc",
    "durationMin": 80,
    "contentMarkdown": "# State Management Avancé avec Bloc\n\n## Introduction\n\nBloc (Business Logic Component) est un pattern de state management qui sépare clairement la logique métier de l'interface utilisateur en utilisant des événements et des états. Il est recommandé par l'équipe Flutter pour les applications complexes.\n\n## 1. Cubit vs Bloc\n\nCubit est une version simplifiée utilisant des fonctions émettant des états. Bloc utilise des événements typés pour une traçabilité maximale.\n\n```dart\n// Cubit — simple, basé sur des fonctions\nclass CounterCubit extends Cubit<int> {\n  CounterCubit() : super(0);\n\n  void increment() => emit(state + 1);\n  void decrement() => emit(state - 1);\n}\n\n// Bloc — basé sur des événements typés\n// Événements\nsealed class AuthEvent {}\nclass LoginEvent extends AuthEvent {\n  final String email;\n  final String password;\n  LoginEvent(this.email, this.password);\n}\nclass LogoutEvent extends AuthEvent {}\n\n// États\nsealed class AuthState {}\nclass AuthInitial extends AuthState {}\nclass AuthLoading extends AuthState {}\nclass AuthAuthenticated extends AuthState {\n  final String userId;\n  AuthAuthenticated(this.userId);\n}\nclass AuthError extends AuthState {\n  final String message;\n  AuthError(this.message);\n}\n\n// Bloc\nclass AuthBloc extends Bloc<AuthEvent, AuthState> {\n  final AuthRepository _repository;\n\n  AuthBloc(this._repository) : super(AuthInitial()) {\n    on<LoginEvent>(_onLogin);\n    on<LogoutEvent>((event, emit) => emit(AuthInitial()));\n  }\n\n  Future<void> _onLogin(LoginEvent event, Emitter<AuthState> emit) async {\n    emit(AuthLoading());\n    try {\n      final userId = await _repository.login(event.email, event.password);\n      emit(AuthAuthenticated(userId));\n    } catch (e) {\n      emit(AuthError(e.toString()));\n    }\n  }\n}\n```\n\n## 2. BlocProvider et BlocListener\n\nBlocProvider rend un Bloc accessible à tous les widgets enfants via l'arbre de widgets. BlocListener exécute des actions (navigation, snackbar) en réponse aux changements d'état.\n\n```dart\n// Injection du Bloc\nvoid main() {\n  runApp(\n    BlocProvider(\n      create: (context) => AuthBloc(AuthRepository()),\n      child: const MyApp(),\n    ),\n  );\n}\n\n// Utilisation dans un widget\nclass LoginScreen extends StatelessWidget {\n  const LoginScreen({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return BlocListener<AuthBloc, AuthState>(\n      listener: (context, state) {\n        if (state is AuthAuthenticated) {\n          Navigator.pushReplacementNamed(context, '/home');\n        } else if (state is AuthError) {\n          ScaffoldMessenger.of(context).showSnackBar(\n            SnackBar(content: Text(state.message)),\n          );\n        }\n      },\n      child: BlocBuilder<AuthBloc, AuthState>(\n        builder: (context, state) {\n          if (state is AuthLoading) {\n            return const CircularProgressIndicator();\n          }\n          return LoginForm();\n        },\n      ),\n    );\n  }\n}\n```\n\n## 3. Architecture avec Bloc\n\nSéparez l'application en couches :\n- **Data** : repositories, datasources (API, base de données)\n- **Business Logic** : blocs (gestion des événements et états)\n- **Presentation** : widgets (UI pure, sans logique métier)\n\n```
lib/\n  data/\n    repositories/      # AuthRepository, ProductRepository\n    datasources/       # ApiDataSource, LocalDataSource\n  business_logic/\n    blocs/             # AuthBloc, ProductBloc\n    cubits/            # CounterCubit\n  presentation/\n    screens/           # LoginScreen, HomeScreen\n    widgets/           # LoginForm, ProductCard\n```\n\n## 4. Test des blocs\n\nLes blocs sont facilement testables unitairement grâce au package `bloc_test` :\n\n```dart\nvoid main() {\n  group('AuthBloc', () {\n    late AuthRepository repository;\n\n    setUp(() {\n      repository = MockAuthRepository();\n    });\n\n    blocTest<AuthBloc, AuthState>(\n      'émet [Loading, Authenticated] pour un login valide',\n      build: () => AuthBloc(repository),\n      act: (bloc) => bloc.add(LoginEvent('test@test.com', 'password')),\n      expect: () => [AuthLoading(), AuthAuthenticated('user-1')],\n    );\n\n    blocTest<AuthBloc, AuthState>(\n      'émet [Loading, Error] pour un login invalide',\n      build: () => AuthBloc(repository),\n      act: (bloc) => bloc.add(LoginEvent('bad', 'credentials')),\n      expect: () => [AuthLoading(), AuthError('Invalid credentials')],\n    );\n  });\n}\n```\n\n## Résumé\n\n- Bloc : pattern événements/états avec typage fort et traçabilité\n- Cubit : version simplifiée pour les cas simples\n- BlocProvider/BlocListener/BlocBuilder : intégration widgets\n- Architecture en couches (Data → Business Logic → Presentation)\n- Testable unitairement avec blocTest"
  },
  {
    "title": "Firebase en Flutter",
    "durationMin": 80,
    "contentMarkdown": "# Firebase en Flutter\n\n## Introduction\n\nFirebase fournit une suite de services backend clés en main pour les applications mobiles : authentification, base de données, stockage et notifications.\n\n## 1. Firebase Auth\n\nAuthentifiez les utilisateurs avec email/mot de passe, Google, Apple ou anonyme. Gérez les sessions avec des tokens.\n\n## 2. Cloud Firestore\n\nBase de données NoSQL temps réel. Stockez des documents dans des collections. Sécurisez avec des règles Firestore.\n\n## 3. Firebase Storage\n\nStockez et récupérez des fichiers utilisateur (images, vidéos). Intégrez avec l'appareil photo et la galerie.\n\n## 4. Cloud Messaging (FCM)\n\nEnvoyez des notifications push aux utilisateurs. Gérez les permissions et les canaux de notification.\n\n## Résumé\n\n- Firebase Auth : authentification multiple\n- Cloud Firestore : base de données NoSQL temps réel\n- Firebase Storage : stockage de fichiers\n- FCM : notifications push"
  },
  {
    "title": "Animations et Widgets Custom",
    "durationMin": 75,
    "contentMarkdown": "# Animations et Widgets Custom\n\n## Introduction\n\nLes animations rendent une application Flutter vivante et professionnelle. Maîtrisez AnimationController, Tween, Hero et CustomPainter.\n\n## 1. AnimationController et Tween\n\nAnimationController pilote l'animation dans le temps. Tween définit la plage de valeurs. ImplicitlyAnimatedWidget pour les animations simples.\n\n## 2. Hero et transitions\n\nHero crée des animations de transition entre deux écrans. PageRouteBuilder pour des transitions personnalisées.\n\n## 3. Staggered animations\n\nEnchaînez plusieurs animations avec AnimationController et Interval. Créez des effets complexes fluides.\n\n## 4. CustomPainter\n\nDessinez des formes personnalisées avec CustomPainter et Canvas. Créez des graphiques, des illustrations et des widgets originaux.\n\n## Résumé\n\n- AnimationController + Tween : animations pilotées\n- Hero : transitions entre écrans\n- Staggered : animations en cascade\n- CustomPainter : dessin personnalisé sur Canvas"
  },
  {
    "title": "Tests et CI/CD",
    "durationMin": 65,
    "contentMarkdown": "# Tests et CI/CD\n\n## Introduction\n\nLes tests sont essentiels pour maintenir la qualité d'une application Flutter. Une stratégie de test complète inclut les tests unitaires, de widgets et d'intégration.\n\n## 1. Tests unitaires\n\nTestez les modèles, les repositories et les blocs isolément. Utilisez mockito pour simuler les dépendances.\n\n## 2. Tests de widgets\n\nTestez l'interface utilisateur avec WidgetTester. Vérifiez l'affichage, les interactions et la navigation.\n\n## 3. Tests d'intégration\n\nTestez l'application complète avec IntegrationTest. Simulez des parcours utilisateur complets.\n\n## 4. CI/CD avec Codemagic et Fastlane\n\nAutomatisez les tests et le déploiement avec Codemagic (CI cloud) et Fastlane (automatisation des stores).\n\n## Résumé\n\n- Tests unitaires : logique métier\n- Tests de widgets : UI et interactions\n- Tests d'intégration : parcours complets\n- Codemagic + Fastlane : CI/CD automatisé"
  }
],
  project: {
  "title": "Application de Suivi de Dépenses avec Firebase",
  "instructions": "# Application de Suivi de Dépenses — Expense Tracker\n\n## Objectif\n\nCréer une application Flutter complète pour suivre ses dépenses personnelles avec authentification Firebase, base de données temps réel, animations fluides et architecture BLoC.\n\n## Consignes\n\n### 1. Fonctionnalités obligatoires\n\n- **Authentification** : inscription / connexion / déconnexion avec Firebase Auth\n- **CRUD des dépenses** : ajouter, modifier, supprimer des dépenses (montant, catégorie, date, note)\n- **Catégories** : alimentation, transport, loisirs, logement, santé, autre — avec icônes et couleurs\n- **Tableau de bord** : total du mois, dépenses par catégorie, graphique camembert\n- **Recherche et filtres** : par catégorie, par plage de dates, par montant\n- **Notifications push** : rappel mensuel pour saisir les dépenses\n\n### 2. Architecture BLoC\n\n- **AuthBloc** : gère l'état d'authentification (connecté/déconnecté)\n- **ExpenseBloc** : gère la liste des dépenses (ajout/modification/suppression)\n- **DashboardBloc** : calcule les statistiques (total, moyenne, par catégorie)\n- **Événements** : ChargerDepenses, AjouterDepense, ModifierDepense, SupprimerDepense\n- **États** : Initial, Chargement, Charge, Erreur\n\n### 3. Composants obligatoires\n\n- **ExpenseCard** : carte de dépense avec montant, catégorie (icône couleur), date\n- **CategoryPicker** : sélecteur de catégorie avec grille d'icônes\n- **DashboardHeader** : en-tête avec total du mois et tendance (flèche haut/bas)\n- **ExpenseChart** : graphique camembert des catégories (CustomPainter)\n- **AddExpenseSheet** : bottom sheet de création avec formulaire validé\n\n### 4. Firebase\n\n- **Firebase Auth** : email/mot de passe\n- **Cloud Firestore** : collection `users/{uid}/expenses` pour les données personnelles\n- **Firebase Messaging** : notification push de rappel mensuel\n- **Firebase Storage** : upload de justificatifs (optionnel)\n\n### 5. Animations\n\n- **Staggered animation** sur la liste des dépenses au chargement\n- **Hero** sur la transition carte → écran de détails\n- **AnimatedContainer** pour l'animation du dashboard\n- **CustomPainter** pour le graphique camembert\n\n### 6. Tests\n\n- **Widget tests** : test du formulaire d'ajout de dépense et de la carte de dépense\n- **Unit tests** : test du calcul des totaux et de la logique BLoC\n- **Integration test** : parcours complet connexion → ajout dépense → visualisation graphique\n\n### 7. CI/CD\n\n- Pipeline GitHub Actions ou Codemagic\n- Étapes : lint → tests → build → publication\n\n## Contraintes techniques\n\n- **Flutter 3.19+** avec Dart null safety\n- **flutter_bloc** pour la gestion d'état\n- **firebase_core**, **firebase_auth**, **cloud_firestore** pour Firebase\n- **equatable** pour les comparaisons d'états\n- **CustomPainter** pour le graphique (pas de package externe)\n- Les données doivent être liées à l'utilisateur Firebase authentifié\n\n## Critères d'évaluation\n\n- **Fonctionnalités** : authentification, CRUD dépenses, tableau de bord, catégories, filtres\n- **Architecture BLoC** : séparation claire événements/états/blocs, aucun code métier dans les widgets\n- **Firebase** : authentification fonctionnelle, données stockées dans Firestore avec sécurité\n- **Animations** : staggered, hero, AnimatedContainer et CustomPainter pour le graphique\n- **Tests** : couverture minimale des widget tests et unit tests, pipeline CI/CD configuré",
  "evaluationCriteria": [
    "Fonctionnalités : authentification, CRUD dépenses, tableau de bord, catégories et filtres fonctionnels",
    "Architecture BLoC : séparation claire événements/états/blocs, aucun code métier dans les widgets",
    "Firebase : authentification fonctionnelle, données stockées dans Firestore, structure users/{uid}/expenses",
    "Animations : staggered, hero, AnimatedContainer et CustomPainter pour le graphique camembert",
    "Tests : couverture minimale des widget tests et unit tests, pipeline CI/CD configuré"
  ]
},
};
