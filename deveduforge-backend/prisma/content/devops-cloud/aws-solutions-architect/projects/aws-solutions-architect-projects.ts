const project = {
  "title": "Architecture Microservices HA sur AWS ECS avec Event-Driven Pipeline",
  "instructions": "# Architecture Microservices HA sur AWS ECS avec Event-Driven Pipeline\n\n## Objectif\n\nConcevoir et déployer une architecture microservices hautement disponible sur AWS ECS Fargate, avec communication asynchrone via EventBridge, file d'attente SQS, et déchiffrement des secrets via KMS.\n\n## Consignes\n\n### 1. Réseau VPC HA\n\n- Créer une VPC avec CIDR 10.0.0.0/16\n- 2 sous-réseaux publics (eu-west-1a, eu-west-1b) pour les ALB\n- 2 sous-réseaux privés (eu-west-1a, eu-west-1b) pour ECS Fargate\n- Internet Gateway pour les sous-réseaux publics\n- NAT Gateway dans un subnet public pour les sous-réseaux privés\n- Security Groups :\n  - ALB SG : inbound 80/443 depuis 0.0.0.0/0\n  - ECS SG : inbound 3000/8080 uniquement depuis ALB SG\n\n### 2. ECS Fargate Cluster avec 2 Microservices\n\n- Créer un cluster ECS Fargate\n- Service 1 : API Commande (port 3000)\n  - Task Definition avec image ECR, Fargate, 512 CPU / 1024 MB\n  - Healthcheck HTTP /health\n  - Service Discovery via Cloud Map\n  - Auto Scaling : min 2, max 6, target tracking CPU 50%\n- Service 2 : Service Notification (port 8080)\n  - Consomme les messages de la file SQS\n  - Envoie des notifications (simulé par un log)\n- ALB avec target groups séparés pour chaque service\n- Routes : /api/* → Commande, /notifications/* → Notification\n\n### 3. Communication Asynchrone (Event-Driven)\n\n- Queue SQS pour les événements de commande\n- Topic SNS pour les notifications email\n- Rule EventBridge qui route les événements de type 'OrderCreated' vers la queue SQS\n- Lambda (ou ECS Task) qui poll SQS et traite les événements\n- Step Function pour orchestrer le workflow de traitement de commande :\n  Valider → Payer → Envoyer Notification → Mettre à jour Statut\n\n### 4. Sécurité et Conformité\n\n- Clé KMS pour le chiffrement des données sensibles\n- Secrets Manager pour les credentials DB (passés aux tasks ECS via 'secrets')\n- CloudTrail activé pour l'audit des actions API\n- GuardDuty activé pour la détection de menaces\n- Toutes les ressources taguées (Environment=prod, Project=order-service, Team=backend)\n\n### 5. Monitoring et Optimisation des Coûts\n\n- CloudWatch Dashboard montrant : ECS CPU/Memory, ALB Request Count, SQS Queue Depth\n- CloudWatch Alarm sur SQS ApproximateNumberOfMessages > 100\n- Cost Explorer pour analyser les coûts\n- Savings Plans Recommendation pour les instances ECS (Compute Savings Plan)\n- Budget AWS avec alerte email à 80%\n\n## Contraintes techniques\n\n- Toute l'infrastructure en CLI AWS ou CloudFormation/Terraform\n- ECS Fargate uniquement (pas de serveurs EC2)\n- L'API Commande doit être en TypeScript/Node.js\n- Les secrets ne doivent jamais apparaître en clair dans les logs ou les configurations\n- Au moins 2 AZ pour chaque service critique\n\n## Livrables\n\n1. Scripts d'infrastructure (CLI/CloudFormation/Terraform) pour VPC, ECS, ALB, SQS, EventBridge\n2. Code source de l'API Commande (TypeScript) avec Dockerfile\n3. Task Definitions ECS pour les 2 services\n4. Workflow Step Functions (JSON)\n5. Dashboard CloudWatch et alarms configurés\n6. README.md avec architecture complète, diagramme et guide de déploiement",
  "evaluationCriteria": [
    "Architecture VPC HA (2 AZ, sous-réseaux publics/privés, NAT Gateway, Security Groups correctement configurés)",
    "ECS Fargate opérationnel (2 services, ALB routing, Auto Scaling, healthcheck, Service Discovery)",
    "Communication asynchrone (SQS + EventBridge + Step Functions orchestration du workflow)",
    "Sécurité (KMS, Secrets Manager via ECS secrets, CloudTrail, GuardDuty, tagging complet)",
    "Monitoring et coûts (CloudWatch Dashboard, Alarms, Cost Explorer, Savings Plans)"
  ]
};

export default project;
