import { Level } from '@prisma/client';

export const nodejsMicroservices = {
  techSlug: "nodejs",
  title: "Node.js : Microservices",
  description: "Concevez, déployez et orchestrez des architectures microservices robustes avec Node.js, Docker et les patterns de communication distribuée.",
  level: Level.advanced,
  durationMin: 300,
  lessons: [
  {
    "title": "Architecture Microservices",
    "durationMin": 75,
    "contentMarkdown": "# Architecture Microservices\n\n## Introduction\n\nLes microservices sont une approche architecturale qui décompose une application monolithique en petits services indépendants, chacun gérant une fonctionnalité spécifique. Cette leçon couvre les principes fondamentaux et vous montre comment créer un microservice Express minimal.\n\n## 1. Monolithe vs Microservices\n\nUn monolithe est une application unique où tous les composants sont étroitement couplés. Une architecture microservices découpe ces composants en services autonomes.\n\n| Aspect | Monolithe | Microservices |\n|--------|-----------|---------------|\n| Déploiement | Un seul artefact | Un artefact par service |\n| Scaling | Vertical (plus de RAM/CPU) | Horizontal (plus d'instances) |\n| Isolation | Un bug peut tout casser | Pannes isolées |\n| Stack technique | Uniforme | Polyglotte |\n\n## 2. Créer un microservice avec Express\n\nVoici un microservice de gestion d'utilisateurs minimal :\n\n```typescript\nimport express from 'express';\nimport { createClient } from 'redis';\n\nconst app = express();\napp.use(express.json());\n\ninterface User {\n  id: string;\n  name: string;\n  email: string;\n}\n\n// Base de données en mémoire (remplacer par PostgreSQL en production)\nconst users: Map<string, User> = new Map();\n\n// Routes REST\napp.get('/users', (_req, res) => {\n  res.json({ users: Array.from(users.values()) });\n});\n\napp.get('/users/:id', (req, res) => {\n  const user = users.get(req.params.id);\n  if (!user) return res.status(404).json({ error: 'User not found' });\n  res.json(user);\n});\n\napp.post('/users', (req, res) => {\n  const { id, name, email } = req.body;\n  users.set(id, { id, name, email });\n  res.status(201).json({ id, name, email });\n});\n\n// Health check pour le service discovery\napp.get('/health', (_req, res) => {\n  res.json({ status: 'UP', service: 'user-service', timestamp: new Date().toISOString() });\n});\n\napp.listen(3001, () => {\n  console.log('User Service running on port 3001');\n});\n```\n\n## 3. Bounded Contexts\n\nUn bounded context définit une frontière claire autour d'un sous-domaine métier. Chaque microservice correspond à un bounded context avec son propre modèle de données et sa propre base.\n\n```typescript\n// Context \"Utilisateurs\" — User Service\ninterface User {\n  id: string;\n  email: string;\n  profile: { firstName: string; lastName: string };\n}\n\n// Context \"Commandes\" — Order Service\ninterface Order {\n  id: string;\n  userId: string;  // Seul l'ID utilisateur est partagé\n  products: Array<{ productId: string; quantity: number }>;\n  total: number;\n  status: 'pending' | 'paid' | 'shipped';\n}\n\n// Context \"Produits\" — Product Service\ninterface Product {\n  id: string;\n  name: string;\n  price: number;\n  stock: number;\n}\n```\n\n## 4. Communication entre services\n\n- **Synchrone** : HTTP REST ou gRPC (appels directs avec attente de réponse)\n\n```typescript\n// Appel synchrone entre services avec axios\nimport axios from 'axios';\n\nasync function getOrderWithUser(orderId: string) {\n  const order = await axios.get(`http://order-service:3003/orders/${orderId}`);\n  const user = await axios.get(`http://user-service:3001/users/${order.data.userId}`);\n  return { ...order.data, user: user.data };\n}\n```\n\n- **Asynchrone** : Message brokers (RabbitMQ, Kafka) pour les événements\n\n```typescript\n// Publication d'un événement (producteur)\nimport amqp from 'amqplib';\n\nasync function publishOrderCreated(order: Order) {\n  const conn = await amqp.connect('amqp://rabbitmq:5672');\n  const channel = await conn.createChannel();\n  await channel.assertExchange('orders', 'topic', { durable: true });\n  channel.publish('orders', 'order.created', Buffer.from(JSON.stringify(order)));\n  await channel.close();\n}\n\n// Consommation d'un événement (consommateur)\nasync function consumeOrderEvents() {\n  const conn = await amqp.connect('amqp://rabbitmq:5672');\n  const channel = await conn.createChannel();\n  await channel.assertExchange('orders', 'topic', { durable: true });\n  const q = await channel.assertQueue('notification-service', { exclusive: true });\n  await channel.bindQueue(q.queue, 'orders', 'order.*');\n  \n  channel.consume(q.queue, (msg) => {\n    if (msg) {\n      const order = JSON.parse(msg.content.toString());\n      console.log('Notification envoyée pour la commande', order.id);\n      channel.ack(msg);\n    }\n  });\n}\n```\n\n## Résumé\n\n- Microservices = services indépendants, chacun avec sa propre base de données\n- Bounded Context = frontière métier d'un service (modèles distincts)\n- Communication synchrone (HTTP REST, axios) pour les requêtes directes\n- Communication asynchrone (RabbitMQ, events) pour le couplage faible\n- Patterns : API Gateway, Service Discovery, Circuit Breaker, Event Sourcing"
  },
  {
    "title": "API Gateway et Message Brokers",
    "durationMin": 75,
    "contentMarkdown": "# API Gateway et Message Brokers\n\n## Introduction\n\nL'API Gateway est le point d'entrée unique d'un système microservices. Les message brokers (RabbitMQ, Redis Pub/Sub) gèrent la communication asynchrone entre services.\n\n## 1. API Gateway avec Express\n\nUn API Gateway gère le routage, l'authentification, le rate limiting et la transformation des requêtes avant qu'elles n'atteignent les microservices.\n\n## 2. RabbitMQ pour la communication asynchrone\n\nRabbitMQ implémente le protocole AMQP. Les producteurs envoient des messages vers des exchanges, qui les distribuent aux queues via des bindings.\n\n## 3. Redis Pub/Sub\n\nRedis Pub/Sub est plus léger que RabbitMQ mais ne persiste pas les messages. Idéal pour les notifications en temps réel.\n\n## 4. Patterns de résilience\n\nCircuit Breaker, Retry avec backoff exponentiel, Timeout, Bulkhead. Implémentez ces patterns pour éviter les pannes en cascade.\n\n## Résumé\n\n- API Gateway : routage, auth, rate limiting, transformation\n- RabbitMQ : persistant, routing complexe, exchanges/queues\n- Redis Pub/Sub : léger, temps réel, pas de persistance\n- Patterns résilience : Circuit Breaker, Retry, Timeout"
  },
  {
    "title": "Sécurité et Authentification Distribuée",
    "durationMin": 75,
    "contentMarkdown": "# Sécurité et Authentification Distribuée\n\n## Introduction\n\nDans une architecture microservices, la sécurité doit être centralisée et cohérente. Chaque service doit pouvoir vérifier l'identité d'un utilisateur sans contacter une base de données à chaque requête.\n\n## 1. JWT avec clés RSA\n\nUtilisez des clés asymétriques : le service d'auth signe les tokens avec la clé privée, les autres services vérifient avec la clé publique.\n\n## 2. OAuth2 et SSO\n\nOAuth2 permet à un utilisateur de s'authentifier via un fournisseur tiers. Le SSO (Single Sign-On) permet d'utiliser la même session sur plusieurs services.\n\n## 3. Rate limiting centralisé\n\nL'API Gateway applique des limites de requêtes pour protéger les services contre les attaques DDoS et les abus.\n\n## 4. Sécurité des communications inter-services\n\nTLS mutuel (mTLS), tokens de service, réseau isolé en Docker. Chaque service doit avoir un identifiant unique.\n\n## Résumé\n\n- JWT RSA : signature asymétrique, pas de partage de secret\n- OAuth2 : flux standardisé pour l'authentification\n- Rate limiting : protection contre les abus\n- mTLS : communication chiffrée entre services"
  },
  {
    "title": "Monitoring et Déploiement",
    "durationMin": 75,
    "contentMarkdown": "# Monitoring et Déploiement\n\n## Introduction\n\nLe monitoring et le déploiement sont essentiels pour maintenir la fiabilité d'une architecture microservices. Chaque service doit exposer des métriques, des logs et des health checks.\n\n## 1. Logs centralisés avec Winston\n\nUtilisez Winston avec des transports variés (console, fichier, ELK). Chaque log doit inclure un corrélation ID pour tracer une requête à travers les services.\n\n## 2. Métriques et alertes\n\nPrometheus collecte les métriques de chaque service. Grafana visualise les tableaux de bord. Des alertes sont configurées pour les anomalies.\n\n## 3. Health checks\n\nChaque service expose un endpoint /health qui vérifie la connexion à la base de données, Redis, et les dépendances.\n\n## 4. Docker Compose multi-service\n\nUn fichier docker-compose.yml orchestre tous les services avec leurs bases de données dédiées, réseaux isolés et variables d'environnement.\n\n## Résumé\n\n- Logs centralisés avec contexte de corrélation\n- Prometheus + Grafana pour les métriques\n- Health checks pour le service discovery\n- Docker Compose pour l'orchestration locale"
  }
],
  project: {
  "title": "Plateforme E-commerce Microservices",
  "instructions": "# Plateforme E-commerce Microservices\n\n## Objectif\n\nConcevoir et implementer une architecture microservices complete pour une plateforme e-commerce. Le systeme doit gerer les utilisateurs, les produits, les commandes et les notifications comme services independants communiquant via HTTP et RabbitMQ.\n\n---\n\n## Architecture Requise\n\n### Services\n\n1. **API Gateway** (port 8080)\n   - Point d'entree unique pour tous les clients\n   - Authentification JWT centralisee (cles RSA)\n   - Rate limiting genere (100 req/15 min)\n   - Proxy vers les microservices via http-proxy-middleware\n\n2. **User Service** (port 3001)\n   - CRUD des utilisateurs\n   - Inscription, connexion, refresh token\n   - Base PostgreSQL dediee\n   - Health check : GET /health\n\n3. **Product Service** (port 3002)\n   - CRUD des produits avec pagination et recherche\n   - Gestion du stock (quantite disponible)\n   - Cache Redis pour les produits populaires\n   - Health check : GET /health\n\n4. **Order Service** (port 3003)\n   - Creation de commandes (verifie stock + paiement)\n   - Historique des commandes par utilisateur\n   - Publie un evenement ORDER_CREATED sur RabbitMQ\n   - Base PostgreSQL dediee\n\n5. **Notification Service** (port 3004)\n   - Ecoute les evenements ORDER_CREATED via RabbitMQ\n   - Envoie des notifications (simulees avec des logs)\n   - Stocke l'historique des notifications en memoire ou en base\n\n### Communication\n\n- **Synchrone** : Gateway vers Services via HTTP (axios)\n- **Asynchrone** : Order Service vers Notification Service via RabbitMQ\n- **Auth** : JWT RS256 (cle privee pour signer, cle publique pour verifier)\n\n---\n\n## Fonctionnalites Detaillees\n\n### Auth (User Service)\n\n| Route | Methode | Description |\n|-------|---------|-------------|\n| /api/auth/register | POST | Inscription (email, password, name) |\n| /api/auth/login | POST | Connexion, retourne access + refresh token |\n| /api/auth/refresh | POST | Rafraichir l'access token |\n\n- Mot de passe hash avec bcrypt (cost factor 12)\n- Access token : 15 min, signe avec cle privee RSA\n- Refresh token : 7 jours, rotation a chaque utilisation\n\n### Produits (Product Service)\n\n| Route | Methode | Auth | Description |\n|-------|---------|------|-------------|\n| /api/products | GET | Non | Liste paginee (?page=1&limit=10&search=...) |\n| /api/products/:id | GET | Non | Detail d'un produit |\n| /api/products | POST | Oui (admin) | Creer un produit |\n| /api/products/:id | PUT | Oui (admin) | Modifier un produit |\n| /api/products/:id | DELETE | Oui (admin) | Supprimer un produit |\n\n### Commandes (Order Service)\n\n| Route | Methode | Auth | Description |\n|-------|---------|------|-------------|\n| /api/orders | POST | Oui | Creer une commande (verifie stock) |\n| /api/orders | GET | Oui | Mes commandes |\n| /api/orders/:id | GET | Oui | Detail d'une commande |\n\nLa creation de commande publie un evenement ORDER_CREATED sur RabbitMQ.\n\n### Notifications (Notification Service)\n\n| Route | Methode | Auth | Description |\n|-------|---------|------|-------------|\n| /api/notifications | GET | Oui | Mes notifications |\n\nLes notifications sont generees automatiquement via RabbitMQ.\n\n---\n\n## Contraintes Techniques\n\n- Node.js + TypeScript\n- Express pour tous les services\n- Prisma ORM pour les bases PostgreSQL\n- Docker Compose pour l'orchestration\n- RabbitMQ (amqplib) pour la communication asynchrone\n- Redis (ioredis) pour le cache et le rate limiting\n- Winston pour les logs structures (JSON)\n- Health checks sur chaque service (database + redis)\n- OpenTelemetry pour le tracing distribue\n- Un seul Dockerfile par service avec build multi-stage\n\n---\n\n## Structure du Projet\n\n```\necommerce-microservices/\n├── docker-compose.yml\n├── services/\n│   ├── gateway/\n│   │   ├── src/index.ts\n│   │   ├── Dockerfile\n│   │   └── package.json\n│   ├── user-service/\n│   │   ├── src/index.ts\n│   │   ├── prisma/schema.prisma\n│   │   ├── Dockerfile\n│   │   └── package.json\n│   ├── product-service/\n│   │   ├── src/index.ts\n│   │   ├── prisma/schema.prisma\n│   │   ├── Dockerfile\n│   │   └── package.json\n│   ├── order-service/\n│   │   ├── src/index.ts\n│   │   ├── prisma/schema.prisma\n│   │   ├── Dockerfile\n│   │   └── package.json\n│   └── notification-service/\n│       ├── src/index.ts\n│       ├── Dockerfile\n│       └── package.json\n└── shared/\n    ├── logger.ts\n    └── auth.ts\n```\n\n---\n\n## Semaine de Test\n\n1. Creer un utilisateur via /api/auth/register\n2. Se connecter et obtenir un token\n3. Creer des produits (en tant qu'admin)\n4. Creer une commande\n5. Verifier que la notification est generee automatiquement\n6. Tester les health checks de chaque service\n7. Verifier les logs dans la console (format JSON)",
  "evaluationCriteria": [
    "Architecture (services bien decouples, communication synchrone et asynchrone fonctionnelle)",
    "Securite (JWT RSA, auth centralisee, rate limiting, gestion des erreurs 401/403)",
    "Resilience (health checks, gestion des pannes, retry sur les appels inter-services)",
    "Observabilite (logs Winston avec contexte, tracing OpenTelemetry, health endpoints)",
    "Deploiement (Docker Compose complet, bases dediees, reseau isole, variables d'env)"
  ]
},
};
